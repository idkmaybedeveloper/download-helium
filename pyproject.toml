[project]
name = "download-helium"
version = "1.0.0"
description = "the easiest way to download the latest helium builds"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
